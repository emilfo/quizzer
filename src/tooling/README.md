# Tooling

Implementation root for repo automation and support utilities.

Suggested subdirectories:

- `context/`
- `routing/`
- `plans/`
- `verification/`
- `observability/`
- `policies/`

Keep this tree executable and inspectable. Avoid letting support code overshadow the main quiz product in `app/`, `lib/`, `supabase/`, and `tests/`.
