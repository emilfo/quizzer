# Lane 5 — Playful Signal keyframes

Static HTML/CSS artifact set for screenshot capture.

## Open

- `index.html`

## Included exports

See `manifest.json` for the full list of 10 `data-export` filenames.

## Notes

- Self-contained: no JS required.
- Each `<section>` maps to one export sheet.
- Stable section ids match the export order.
